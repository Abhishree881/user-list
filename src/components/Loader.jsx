import React from "react";
import "../assets/styles/loader.css"; // Import the CSS file

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
      Loading...
    </div>
  );
};

export default Loader;
